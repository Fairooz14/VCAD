# Normalize text line endings to LF in the repository (checked out natively).
* text=auto eol=lf

# Treat binary assets as binary so Git never rewrites their contents.
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.ico  binary
*.woff  binary
*.woff2 binary
